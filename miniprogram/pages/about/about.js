import config from '../../config/config';

Page({
  data: {
    picUrl: config.logoGif,
    appName: config.miniApp,
    appDesc: config.miniAppDesc,
    slogan: config.miniSlogan,
    version: config.version,
  }
})
